openapi: 3.0.0
info:
  title: Mock API Example
  version: '1.0'
paths:
  /api/v1/placeholder:
    get:
      summary: Example placeholder endpoint
      responses:
        '200':
          description: Successful mocked response
          content:
            application/json:
              example:
                message: "Hello from mocked API!"
